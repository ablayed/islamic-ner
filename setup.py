from setuptools import find_packages, setup

setup(
    name="islamic-ner",
    version="0.1.0",
    description="Islamic NER and relation extraction project",
    packages=find_packages(),
    python_requires=">=3.9",
)
